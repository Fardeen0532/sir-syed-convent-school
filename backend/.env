# Sir Syed Convent School – Production Environment Configuration

# Server
PORT=5000
NODE_ENV=production

# JWT Secret (CHANGE THIS in real deployment!)
JWT_SECRET=sir-syed-convent-prod-jwt-secret-2026!

# Database path (relative to backend/)
# DB_PATH is configured in db.js to resolve from workspace root
